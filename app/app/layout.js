export const metadata = {
  title: "Rationalize - The Mysterious AI Agent",
  description: "An AI agent that analyzes Twitter sentiment on Solana contract addresses.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
